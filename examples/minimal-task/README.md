# Minimal Manual Task

This is the smallest no-runtime example for understanding VALP task evidence.

Run:

```bash
bin/valp audit examples/minimal-task
```

This example uses Manual Mode. It records manual attestation and expected
evidence, but it does not claim automatic dispatch submission, agent status
waits, pane proof, or Full Mode runtime receipts.
