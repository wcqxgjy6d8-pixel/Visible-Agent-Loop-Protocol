# Dispatch: codex

Task: `REAL-DOC-CALIBRATION-2026-07-06`

Calibrate the public VALP documentation after external critique.

Required work:

- Add a clear current-status section that separates proved repository evidence
  from unproven runtime and production claims.
- Correct the HERDR boundary: HERDR is a public reference runtime, not the VALP
  protocol and not a closed-source dependency.
- Add an explicit coverage matrix for current tests and missing live-runtime
  E2E areas.
- Keep first-class non-HERDR adapters and live Full Mode examples on the
  roadmap.
- Record this as a sanitized real Manual Mode case study without claiming Full
  Mode runtime proof.

Expected evidence:

- `agents/codex/evidence.md`
- `agents/codex/review.md`
- `evidence/verification.md`
