# Codex Evidence

Implemented the documentation calibration as a real repository task.

Changed public documentation to:

- add `docs/project-status.md` with current package state, repository proof,
  reference-runtime boundary, known gaps, promotion language, and coverage
  matrix;
- add a `Current Status` section to `README.md`;
- clarify HERDR source status in `docs/runtime-adapters.md`,
  `docs/comparison.md`, and `docs/faq.md`;
- update install/runtime/platform source checks to include the public HERDR
  repository;
- update `ROADMAP.md` and `CHANGELOG.md` so credibility work is visible.

Verification evidence is recorded in `evidence/verification.md`.

This evidence does not claim live HERDR dispatch, Full Mode runtime proof, or
production deployment reliability.
